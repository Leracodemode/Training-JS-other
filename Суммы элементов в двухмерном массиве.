function sumsInArray(arr){
  let sum = 0;
  let a = [];
  for (let i = 0; i < arr.length; i++){
    sum = 0;
    for (let j = 0; j < arr[i].length; j++){
      sum = sum + arr[i][j];
    }
    a.push(sum);
  }
  return a;
}
