function firstEvenElement(arr){
  if (arr.length === 0) return [];
  let element = 0;
  let index = 0;
  for (let i  =0; i < arr.length; i++){
    if (arr[i] % 2 === 0){
      element = arr[i];
      index = i;
      break;
    }
  }
  return [element, index]
}
