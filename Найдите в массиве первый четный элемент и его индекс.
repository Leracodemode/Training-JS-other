function firstEvenElement(arr){
  let result = [];
  let element = 0;
  for (let i  =0; i < arr.length; i++){
    if (arr[i] % 2 === 0){
      element = arr[i];
      result = [element, i]
      break;
    }
  }
  return result;
}
