function counOfSquares(arr){
  let count = 0;
  for (let i = 0; i < arr.length; i++) {
    if (Number.isFinite(arr[i]) && arr[i] >= 0 && Math.sqrt(arr[i]) % 1 === 0) {
      count++;
      }
    } 
  return count;
}
