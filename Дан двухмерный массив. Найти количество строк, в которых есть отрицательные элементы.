function countLinesWithNegativeElements(arr){
  let count;
  let rez =0;
  for(let i = 0; i< arr.length; i++){
    count = 0;
  for (let j = 0; j <arr[i].length; j++){
   if(arr[i][j] < 0) count++;
 }
    (count > 0)? rez++ : rez;
 }
   return rez;
 }
